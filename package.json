{
  "name": "base-emr",
  "license": "BSD-3-Clause",
  "scripts": {
    "start": "polymer serve",
    "build": "polymer build",
    "deploy": "firebase deploy",
    "deploy:dev": "polymer build && firebase deploy"
  }
}